spring.application.name=target-app
server.port=8080
management.endpoints.web.exposure.include=health
sftp.host=${SFTP_HOST}
sftp.port=${SFTP_PORT:22}
sftp.username=${SFTP_USERNAME}
sftp.password=${SFTP_PASSWORD}
sftp.folder=${SFTP_FOLDER:/reports}
